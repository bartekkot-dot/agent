# M5 — §4 micro-moments: provider-grid connect + research path-draw

Depends: M2. Source: `MOTION-SPEC.md` §4, §5.

Two independent micro-animations in one item since they're both small, both "once, on
enter" (not scrubbed, not repeated), and both live in sections M2 already created
statically. Split into two commits if convenient, but review together.

## Step 1 — verify before changing

Read `src/components/models-section.tsx` and `src/components/research-section.tsx` as M2
left them, and re-check whatever M2's completion notes said about
`deep-research-diagram.tsx`'s fate (kept-and-adapted vs. deleted-and-rebuilt) — this item
depends on that decision.

## Step 2a — provider grid "connect" (§4a)

On first scroll into view (`whileInView`, Framer Motion, `once: true` — or the existing
`src/hooks/use-in-view.ts` if you prefer consistency with the rest of the codebase over
introducing Framer's own viewport hook; both are reasonable, pick one and don't mix them
within this component), fire a single ~600–800ms sequence: tiles ripple a quick `--live`
pulse outward from the center `APP_NAME` tile (e.g. staggered opacity/scale pulse on each
provider tile keyed by its distance from center), then everything settles to idle
monochrome **except** the center tile stays softly lit (`--live` border/glow, permanently,
not just during the ripple — re-read spec: "settle to idle monochrome with the center mark
softly lit").

Reduced motion: skip the ripple, render immediately with the center tile already lit.

## Step 2b — Deep Research path draw (§4b)

An SVG path (`content.research.steps`: Plan → Search → Read → Compile → Answer — note this
is 5 steps, not the 4 in the existing `deep-research-diagram.tsx`'s `STEPS` array which has
Plan/Search/Read/Synthesize — reconcile the naming, likely rename "Synthesize" → "Compile"
and add "Answer" as a 5th node, matching `content.research.steps` exactly since content.ts
is the single source of truth per G3) that draws left-to-right using the `pathLength`
technique (`stroke-dasharray`/`stroke-dashoffset` animated via Framer Motion's `pathLength`
prop on an SVG `<motion.path>`, or a CSS custom-property + keyframe approach like the
existing `.council-line-animate`/`--dash-length` pattern in `src/index.css` lines ~214–247
— that pattern already does exactly this dash-offset draw technique for
`model-council-diagram.tsx`, so reusing it here keeps the codebase consistent rather than
introducing a second drawing technique). Each node lights `--live` as the line reaches it,
then settles (to what — steady green, per the "settles to steady" language used elsewhere
in the spec for finished-but-not-reverted states; don't fade research nodes to `--ink` the
way the M3 chair card does, that's a different, deliberate "conversation is over" signal
that doesn't apply here).

Fires once on scroll into view, not scrubbed, not repeating. Reduced motion: full path +
all nodes shown static and already-lit immediately, no draw animation.

## Step 3 — verify

`npm run build` clean. Screenshot both sections at rest (before scroll-into-view has
fired, if testable — e.g. page loaded with the section below the fold) and after (scrolled
into view, animation settled), both themes. Confirm neither animation re-fires on repeated
scroll in/out/in (the "once" requirement) — scroll away and back in the test and confirm no
second ripple/draw happens. Reduced-motion screenshots for both.

## Step 4 — report and stop

Show the diff and screenshots. Note the `deep-research-diagram.tsx` reconciliation (adapted
vs. rebuilt, step-label renaming). Do not proceed to M6 in the same turn.
