# M1 — Amend PLAN.md + theme tokens (both modes) + typography check

Depends: none. Source: `MOTION-SPEC.md` §0, §1, §8, §9.1.

## Context

A prior session already did an ad-hoc palette pass: swapped `--brand` from Apple-blue to
green (`#047857` light / `#35c07a` dark), added a subtle green-tinted `oklch` background in
dark mode, and wired Cabinet Grotesk (Fontshare) as `--font-heading` on `h1` + five `h2`s.
None of that is committed to git — it's uncommitted working-tree state.

`MOTION-SPEC.md` §1 is the **authoritative, more complete** token table and supersedes
those ad-hoc values (different light-mode green: `#158A4E`, not `#047857`; explicit hex
canvas/surface/ink/muted values instead of the oklch-tint approach). Don't be alarmed that
the numbers move again — this is expected, not a mistake.

## Step 1 — verify before changing

Read `src/index.css` (the `:root` and `.dark` blocks, ~lines 53–127) and confirm the
current state matches what's described above. Read `PLAN.md` in full (142 lines) and
confirm G6 (extension) still reads as quoted in `MOTION-SPEC.md` §8's note, and that the
deploy-target line still says Vercel. Read `vite.config.ts` and
`.github/workflows/deploy.yml` and confirm they're built for Cloudflare Pages (base `/`
by default, `VITE_BASE_PATH` override for GitHub Pages). If any of this has changed since
this prompt was written, stop and report the discrepancy before proceeding.

## Step 2 — amend PLAN.md

1. Fix the deploy-target line: it currently says "deployed on Vercel" — change every
   mention to Cloudflare Pages (matching the actual `vite.config.ts` comment: "Cloudflare
   Pages serves from the domain root... GitHub Pages (subpath) sets VITE_BASE_PATH").
2. Add a note directly under **G6 (extension)** recording that `MOTION-SPEC.md` (dated
   this session) explicitly supersedes it: the two real demo videos
   (`model-council-demo.mp4`, `deep-research-demo.mp4`) are being retired in favor of the
   new scripted/animated visuals built in M3–M5, per explicit user instruction. Name the
   date and say "superseded, not violated" so a future session doesn't flag M2 onward as
   breaking G6.
3. Leave everything else in PLAN.md alone — this is a targeted correction + note, not a
   rewrite.

## Step 3 — theme tokens

In `src/index.css`, update the existing shadcn-style CSS variables to carry the addendum's
values. **Do not introduce a second, parallel set of variables** named `--canvas`,
`--surface`, `--ink`, `--live`, `--hairline` — that would leave two competing token systems
in one file (a real footgun: some components would silently use the old names). Instead,
map the addendum's concept to the existing variable that already does that job:

| Addendum token | Existing variable | Dark value | Light value |
|---|---|---|---|
| `--canvas` | `--background` | `#0A0B0A` | `#FFFFFF` |
| `--surface` | `--card` (+ `--popover`, same value, for consistency) | `#12140F` | `#F5F7F3` |
| `--ink` | `--foreground` (+ `--card-foreground`, `--popover-foreground`) | `#F1F4EE` | `#101512` |
| `--muted` (their body-text gray) | `--muted-foreground` | `#868D80` | `#5B635A` |
| `--live` | `--brand` | `#35C07A` (unchanged from prior session) | `#158A4E` (was `#047857`) |
| `--hairline` | `--border` (+ `--input`, same value) | `rgba(255,255,255,0.08)` (was 10%, now exactly 8%) | `rgba(0,0,0,0.08)` (was solid `oklch(0.922 0 0)`) |

Leave `--brand-foreground` as-is (`#ffffff` light / `#0a0a0a` dark) — already correct, and
re-derive the contrast math below to confirm it still holds with the new `--live` values:

- Light: white text on `#158A4E` — confirm ≥ 4.5:1 (rough calc puts it ~5.3:1, but verify,
  don't just trust this note).
- Dark: `#0a0a0a` text on `#35C07A` — unchanged from prior session, already verified ~8.5:1.
  **Do not use white text on the dark-mode green** — that combination fails AA badly (~2.3:1,
  verified last session). This is a real trap: it looks fine at a glance because green
  "seems light," but WCAG luminance math says otherwise.

`--sidebar*` variables are unused in this project (no sidebar component exists) — leave
them untouched, don't spend time reconciling them to the new palette.

## Step 4 — the light-mode glow gotcha

Re-read `.glow-orb` / `.glow-orb-animate` / `.dark .glow-orb` in `src/index.css`
(~lines 143–182). The spec's concern: "on light mode a glow is invisible — replace it with
a soft tint fill + drop shadow, never additive bloom."

The existing technique (radial-blurred circle at `bg-brand`, lower opacity in light than
dark) may already read fine — last session's light-mode hero screenshot showed a visible
soft mint glow. **Don't assume it's already solved.** Take a fresh screenshot at the new
`#158A4E` value (see Step 5) and judge it on its own: if the glow reads as a visible,
intentional soft tint, leave the mechanism alone. If it looks washed-out/invisible, adjust
light mode's blur radius down and/or opacity up (don't touch the dark-mode values, which
already work) until it reads as a deliberate tint-and-shadow rather than nothing.

## Step 5 — verify

Run `npm run build` — must pass clean.

Start the dev server and screenshot both themes. There's no `chromium-cli` in this
environment; the working pattern from last session was:

```bash
npm run dev -- --port 5173 &   # then poll curl until it responds
npx --yes playwright screenshot --color-scheme=dark --wait-for-timeout=1500 \
  http://localhost:5173 /path/to/dark.png
# light mode needs localStorage forced (the site's own toggle defaults dark
# regardless of prefers-color-scheme) — write a storageState JSON:
#   {"cookies":[],"origins":[{"origin":"http://localhost:5173",
#     "localStorage":[{"name":"theme","value":"light"}]}]}
npx --yes playwright screenshot --load-storage=/path/to/light-storage.json \
  --color-scheme=light --wait-for-timeout=1500 \
  http://localhost:5173 /path/to/light.png
```

`npx playwright` needs `playwright` actually installed to eval/inspect computed styles
(the screenshot CLI alone doesn't need it); last session did `npm init -y && npm install
playwright@<version matching npx's resolved version>` in a scratch directory for that.

Confirm visually: single green glow behind hero in both themes, no green bleeding into
borders/structural chrome, headings still render in Cabinet Grotesk (unaffected by this
item, but confirm nothing broke).

## Step 6 — report and stop

Show the diff. Report the actual computed contrast ratios you verified (don't just cite
this prompt's estimates), and whether the light-mode glow needed adjustment or was already
fine. **Do not proceed to M2 in the same turn** — this is a stop-after-one-item backlog.
